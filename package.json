{
  "name": "wordgame-fragments-of-language",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "cap:build": "npm run build && npx cap sync",
    "cap:open": "npx cap open android"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
